# Example .env file for ZipApp
# Copy this file to .env and update values **(do not commit real credentials)**

# Standard fallback: DefaultConnection or ConnectionStrings__DefaultConnection
# Example MySQL connection string using your provided credentials:
$env= DefaultConnection=Server=sql7.freesqldatabase.com;Port=3306;Database=sql7808503;User=sql7808503;Password=ZNzeqYbxrG;

# Use this file locally with 'setx' or a local secret manager; in Docker use `--env-file .env` to pass the env vars.
