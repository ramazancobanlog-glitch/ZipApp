using Microsoft.AspNetCore.SignalR;

namespace login.Hubs
{
    public class NotificationHub : Hub
    {
        // No server methods needed for now; hub used for sending notifications from server to clients
    }
}
